# ============================================
# PRODUCTION DEPLOYMENT ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!

# ============================================
# REQUIRED: Security Secrets
# ============================================

# JWT Secret for token signing (REQUIRED - minimum 32 characters)
# Generate with: openssl rand -base64 48
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL

# Database Password (REQUIRED)
# Generate with: openssl rand -base64 32
DB_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# ============================================
# Database Configuration
# ============================================

DB_HOST=timescaledb
DB_PORT=5432
DB_NAME=autoscaler
DB_USER=admin

# ============================================
# API Configuration
# ============================================

API_PORT=8080

# Allowed CORS origins (comma-separated)
# IMPORTANT: Change from '*' to your actual domain(s) in production
ALLOWED_ORIGIN=https://yourdomain.com

# ============================================
# Service Endpoints
# ============================================

# Metrics collector endpoint
COLLECTOR_ENDPOINT=http://simulator:9000/metrics

# Scaler type: 'simulator' or your cloud provider
SCALER_TYPE=simulator

# ============================================
# Optional: Advanced Configuration
# ============================================

# Application mode: 'production', 'development', 'test'
# APP_MODE=production

# Log level: 'debug', 'info', 'warn', 'error'
# APP_LOG_LEVEL=info

# ============================================
# DEPLOYMENT INSTRUCTIONS
# ============================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Generate secrets:
#    JWT_SECRET: openssl rand -base64 48
#    DB_PASSWORD: openssl rand -base64 32
#
# 3. Update ALLOWED_ORIGIN with your actual domain
#
# 4. Deploy:
#    cd deployments
#    docker-compose up -d
#
# 5. Verify:
#    curl http://localhost:8080/health
#
